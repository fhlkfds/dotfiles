# cliphist configuration. Flags here replace the CLI defaults.
# Raised from the default 750; the db lives at ~/.cache/cliphist/db and is a
# plain unencrypted file, so it grows with history (images dominate the size).
max-items 5000
