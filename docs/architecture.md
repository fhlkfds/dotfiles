# Architecture and configuration graph

## Active Hyprland source graph

`hypr/.config/hypr/hyprland.conf` is the entry point. It loads:

```text
hyprland.conf
├── conf/var.conf                 commands and script directory
├── conf/keybinding.conf          keyboard and mouse bindings
├── conf/windows-rules.conf       window behavior and workspace assignment
├── conf/autostart.conf           session processes
├── conf/decorations.conf         generated by the theme tool
├── monitors.conf                 active monitor configuration
└── workspaces.conf               active workspace-to-output mapping
```

`monitors.conf` and `workspaces.conf` are also rewritten by
`auto-monitor-profile.sh` at runtime. The theme tool rewrites
`conf/decorations.conf`. Treat all three as derived/current-state files, not the
best place for durable customization.

## Runtime relationships

```text
Hyprland exec-once
├── quickshell
│   ├── bar and workspace controls
│   ├── dashboard/network/audio/media/display panels
│   ├── freedesktop notification service
│   ├── cliphist browser
│   └── video-download OSD
├── wl-paste watchers → clipboard-store.sh → cliphist
├── hyprpaper ← wallpaper picker / theme tool
├── hypridle → hyprlock, DPMS, system suspend
├── hyprsunset ← night-light-toggle.sh
├── spotify-notify.sh ← playerctl
├── auto-monitor-profile.sh → monitors.conf/workspaces.conf → hyprctl
└── desktop applications assigned to workspaces by rules
```

The top bar is Quickshell. The tracked Waybar configuration is not started by the
current autostart file. Likewise, SwayNC and Noctalia launch commands are retained
as comments rather than active startup directives.

No systemd user unit files are tracked. Session processes are launched through
Hyprland `exec-once`; `systemctl` is used only as an external command for the
configured suspend action.

## Command and data flow examples

### Theme selection

```text
SUPER+T or Quickshell theme panel
        ↓
hypr-theme / generate.py
        ↓
themes/<name>/colors.toml
        ↓
Hyprland, Quickshell, Kitty, Rofi, Hyprlock,
Waybar, Wofi, SwayNC, Noctalia, Zsh, Fastfetch
```

### Wallpaper search

```text
SUPER+SHIFT+W
        ↓
hypr-wallpaper-picker → Quickshell wallpaper panel
        ↓
hypr-wallpaper-picker index/search/apply/activate operations
        ├── local files, first
        └── wallhaven.cc API results, after local results
        ↓
download/validate image → restart hyprpaper → apply cover wallpaper
```

### Browser tools

```text
Chromium extension command
        ↓
Chrome Native Messaging
        ├── copy URL → wl-copy → clipboard watcher/history
        └── download video → yt-dlp → Quickshell OSD → notification/mpv
```

See [Desktop components](./components.md) for implementation details.

### Windows VM

```text
SUPER+ALT+W or Windows.desktop
        ↓
windows-vm launch
        ↓
Docker Compose → Dockur Windows → KVM
        ↓
localhost RDP readiness/authentication probe
        ↓
FreeRDP fullscreen on the focused Hyprland monitor
```

The tracked controller and Compose template are deployed by the `windows` Stow
package. Resource settings and credentials live under `~/.config/windows`, VM
storage under `~/.windows`, and the only guest-visible host data under
`~/Windows`.

## Persistent and generated state

The repository deliberately ignores generated active-theme outputs. The theme
sources are `hypr/.config/hypr/themes/*/colors.toml`; generated destinations
include:

- `hypr/.config/hypr/conf/decorations.conf`
- `hypr/.config/hypr/themes/.active/theme.json`
- `kitty/.config/kitty/theme/current-theme.conf`
- `rofi/.config/rofi/color-themes/current.rasi`
- `hyprlock/.config/hyprlock/colors.conf`
- `waybar/.config/waybar/colors.css`
- SwayNC, Wofi, and Noctalia theme outputs

The generator also writes `~/.config/zsh/current-theme.zsh`, which `.zshrc`
sources. Unlike the component files above, that path is created directly in the
user configuration directory rather than represented by a tracked Stow-package
path.

Runtime state also lives outside Git, mainly below `$XDG_RUNTIME_DIR`,
`$XDG_STATE_HOME` (or `~/.local/state`), and `~/.cache`. Examples include monitor
watcher state, notification history, wallpaper search previews, clipboard data,
and capture recording state.

The optional Windows VM additionally keeps its launch lock and ephemeral
credential environment under `$XDG_RUNTIME_DIR/windows-vm-$UID`. Credentials are
removed after each Compose invocation; persistent credentials remain mode 0600
under `~/.config/windows`.

## Active versus retained components

| Component | Current status | Evidence |
| --- | --- | --- |
| Quickshell | Active | `exec-once = quickshell` |
| Hyprpaper, Hypridle, Hyprsunset | Active | `conf/autostart.conf` |
| Rofi | Active on demand | `SUPER+A` and helper scripts |
| Kitty | Active/default terminal | `conf/var.conf` |
| Waybar | Retained alternative | no active startup command |
| SwayNC | Retained fallback | startup line commented out |
| Wofi | Retained alternative | no active binding/startup |
| Noctalia | Retained configuration | startup line commented out |

This distinction matters during troubleshooting: editing Waybar or SwayNC will
not change the currently visible Quickshell UI.

## Environment variables

Hyprland directly exports `XCURSOR_SIZE=24` and `HYPRCURSOR_SIZE=24`. Scripts also
honor feature-specific variables:

| Variable | Purpose / default |
| --- | --- |
| `HYPR_WALLPAPER_DIR` | wallpaper directory; `/home/liam/Pictures/wallpapers` |
| `HYPR_WALLPAPER_RUNTIME_DIR` | wallpaper search/download runtime data |
| `CHROMIUM_YTDLP_DIR` | browser video destination; `~/Videos` |
| `AI_AGENT_DEFAULT`, `AI_AGENT_CONFIG` | default AI CLI and config path |
| `XDG_RUNTIME_DIR` | transient locks, sockets, and monitor/capture state |
| `XDG_STATE_HOME` | notification state, with `~/.local/state` fallback |
| `SCREENSHOT_DIR`, `SCREENRECORD_DIR` | capture output directories |

The capture suite exposes further tuning variables documented in
[Capture suite](./scripts.md#capture-suite).
